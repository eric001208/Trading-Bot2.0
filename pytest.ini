[pytest]
# The repo may create temporary folders under ./tmp (e.g. during local experiments).
# Some of those folders can end up with restricted permissions on Windows, which
# breaks pytest collection. Ignore tmp entirely.
norecursedirs = tmp

